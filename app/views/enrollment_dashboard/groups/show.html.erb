<h1><%= @group.name %></h1>

<div class="btn-toolbar" role="toolbar">
  <%= link_to "Groups", groups_path, class: "btn btn-default" %>
  <%= link_to "Edit", edit_group_path(@participant), class: "btn btn-default" %>
  <%= link_to "Destroy", @group, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
</div>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Arm:</strong>
  <%= @group.arm_id %>
</p>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
