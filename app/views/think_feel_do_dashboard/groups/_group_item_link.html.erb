<%= link_to "#{group.title} #{arm_type(ThinkFeelDoDashboard::ArmGroupJoin.where(group_id: group.id).first.try(:arm))} <span class=\"badge\">#{Membership.where(group_id: group.id).count}</span>".html_safe, group_path(group), class: "list-group-item" %>