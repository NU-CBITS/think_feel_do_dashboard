<div class="list-group">
  <% groups.each do |group| %>
    <%= link_to "#{group.title} <span class=\"badge\">#{group.participants.count}</span>".html_safe, group_path(group), class: "list-group-item" %>
  <% end %>
</div>